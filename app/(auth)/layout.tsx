/**
 * app/(auth)/layout.tsx
 * Minimal layout for auth pages — no app chrome, just the page.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
